%tr.result
  %td
    -name = result.has_key?("title_field") && !result["title_field"].empty? ? result["title_field"] : result["id"]
    -safe_pid = result['id'].gsub(":", "_")
    -detail_url = catalog_url(result["id"])
    = link_to(name, detail_url)
  %td
    =result["date_t"]
  %td
    =render :partial=>"shared/solr_result_field_value", :collection => result["subject_t"]
  %td
    =result["digital_master_duration_t"]
    